name: 🔄 Auto Update DSA Badges

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 🪄 Refresh Stats
        run: |
          curl -s https://leetcode-stats-api.herokuapp.com/Abhinav_Vengala8 > /dev/null
          curl -s https://gfg-stats-api.onrender.com/user/abhinavvengala8 > /dev/null
      - name: ✅ Commit & Push
        run: |
          git config user.name "GitHub Action"
          git config user.email "actions@github.com"
          git add README.md || true
          git commit -m "🔁 Auto-update DSA stats" || echo "No changes"
          git push